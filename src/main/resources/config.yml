# Vanish Plugin Configuration

# Worlds where vanish works
enabled-worlds:
  - "*"

# Database configuration
database:
  # Type: "sqlite" or "mysql"
  type: "sqlite"

  # SQLite settings
  sqlite:
    path: "plugins/VanishPlugin/vanish.db"

  # MySQL settings
  mysql:
    host: "localhost"
    port: 3306
    database: "vanishdb"
    username: "root"
    password: ""